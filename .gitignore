# Real secrets and real config -- never commit these.
.env
.env.*
!.env.example
config.yaml
!config.example.yaml

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.python-version

# Editors / OS
.vscode/
.idea/
.DS_Store

# Logs / local run artifacts
*.log
.pytest_cache/
docs/images/.trash_slide05

# Lambda example build output -- built locally by examples/aws-lambda/build.sh,
# never committed (this is a full pyiceberg/pyarrow install, ~200MB+).
examples/aws-lambda/build/
*.zip

# Stray renamed/temp scratch files from interactive debugging sessions.
*.stale.*
